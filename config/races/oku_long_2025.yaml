race:
  name: "Okumusashi Long Trail 2025 - 105K"
  distance_km: 105  # placeholder, update after loading GPX
  elevation_gain_m: 8300  # placeholder
  
  # File paths (relative to project root)
  gpx_file: "config/gpx_repo/Okumusashi_Long_Trail_Race_105K_2025.gpx"
  output_file: "results/oku_long_2025_segment_analysis.xlsx"
  
  # Analysis settings
  resample_m: 5  # Resample course to 5 m intervals
  elevation_tolerance_m: 50  # Tolerance for elevation validation

  # ITRA score reference points for this race.
  itra_reference_points:
    - reference_time: "18:34:00"
      reference_score: 729

# No aid station splits defined yet — the whole course is treated as one segment.
aid_stations: []